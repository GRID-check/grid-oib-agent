---
features:
  - >
    Setting up the identity layer behind a new Piloti environment no longer
    means clicking through a dashboard. One command checks the platform
    organization, its owner, sign-in web origins, the observability
    application and feature-flag targeting, reports anything missing or
    different, and can reconcile it — a weekly check now flags drift before
    it reaches your users.
