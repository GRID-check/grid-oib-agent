---
fixes:
  - >
    Research runs that had already finished on the server while the history
    still showed them as running now settle to their real outcome when stopped,
    instead of staying stuck. Stopping a run asks for confirmation first.
