# Feedback Triage Backlog — colleague feedback, verified against code 2026-07-14

> Working file for the feedback-triage loop. Every colleague feedback item was
> verified against the current codebase (branch `claude/feedback-triage-backlog-ko6517`,
> base `develop` @ `f941822`) by parallel code-research agents before being accepted
> here — the feedback predates a very active week (deep-research hardening PR #56,
> UX system audit rounds 1–8, chat-spinner/reconnect fixes), so several reported
> problems no longer exist. Stale items are recorded in "Already addressed" below
> so nobody re-litigates them.
>
> Ranking criterion: user trust for the three beta promises (safe Baurecht research,
> project research, office-wide detail search) first, then effort-to-value.
> IDs are stable (`FB-n`). Re-triaged every cycle; cycle log at the bottom.

## Verdict summary (feedback → code reality)

| Feedback item | Verdict | Backlog |
|---|---|---|
| Chat image upload broken | Never implemented — chat attach = RAG doc pipeline, `.pdf/.docx/.txt/.md` only; no vision-in-chat path | FB-15 |
| Chat very slow / no answer at all | "No answer" **fixed** Jul 10–13 (terminal error frames, 180s watchdog, reconnect-churn fix). "Slow" structurally plausible (shallow tier runs `reasoning_effort: high`, 65k max_tokens, ≤10 LLM turns) | FB-17 |
| Deep Research doesn't work | **Likely fixed** — 5-commit hardening pass merged 2026-07-14 (PR #56): job lifecycle, SSE resume, retry/citation/language, streaming/replay. Needs one human smoke test | — (smoke test) |
| OIB vector DB not active, "only Web Search" | **Backend fine, UI hides it.** KB is queried on every chat turn (`shallow_researcher/register.py:70-75`, config `knowledge_search`), but `ChatThinking.tsx:66-69` filters `knowledge_layer` out of the displayed sources | FB-1 |
| Source labeling DB vs web | Backend has `source_type` (`citation_verification.py:58-66`); frontend `CitationSource` type and cards never carry/render it; KB citations (no URL) can drop out of source lists entirely | FB-2 |
| Precise citations (trace original text) | Partial — KB tool returns file+page+excerpt (`knowledge_layer/src/register.py:343-385`); no click-through from a chat citation to the original passage | FB-4 |
| Restrict web to "better" sources | Not implemented — Tavily config has no domain allowlist; the domain catalog is topic routing, not site quality, and unused in the active config | FB-5 |
| Status display while AI works | **Done** — `ChatThinking.tsx` shows live working state, per-step tool activity, waiting/interrupted/done states | — |
| "AI can make mistakes" notice | Exists only on `/legal/[slug]` (AI-Act Art. 50 text); nothing near the chat input | FB-3 |
| Design calmer/reduced like prototype (incl. Files) | Largely addressed after feedback date: `docs/design/grid-design-language.md` (2026-07-05) + UX audit rounds 1–8 polished all surfaces incl. Files. Prototype is external → final call needs a human visual diff | — (human check) |
| Sidebar: slim down; Overview → Settings; Members → Settings | Still valid — no project Settings page exists at all; Overview & Members are top-level nav items | FB-9 |
| Research not a tab, but part of chat history ("Deep Research" label) | Still valid — separate `/research` tab + `ResearchRunsList` persists; `SessionsPanel` already shows per-session research status icons (half the work exists) | FB-10 |
| Workflows page | Doesn't exist, no spec | FB-11 (needs product) |
| Archive page ("good knowledge" from past projects) | Doesn't exist; org-scoped Project Memory is an adjacent substrate but panel-only, per-project | FB-12 (needs product) |
| Wizard: plan upload (FWP/BBP) + content verification | Still valid — wizard is pure Q&A, no file input; zoning/deviation fields are self-reported | FB-14 |
| Wizard: revise questions | Ongoing — `intake-definition.ts` is versioned (v2) and actively iterated; need the *specific* question changes from the feedback author | — (needs specifics) |
| Wizard: two-stage final check (AI conflict check + human review) | Still valid — `generate_summary.py` only writes one prose sentence; no conflict validation, no review gate | FB-13 |
| Files: show/edit document labels | Still valid — richer ingestion metadata exists but the BFF strips it ("Internal metadata never leaves the BFF", `documents/service.ts:156-170`); UI shows only status/type/size | FB-8 |
| Files: larger preview | Still valid — Files preview is a fixed 384px pane; the big `PdfViewerDialog` (85vh/95vw) already exists but isn't wired into Files | FB-7 |
| Where is the overview of all Output Cards? | Still valid — cards render only inline in chat/reports; only aggregate is a dev-only fixture page | FB-16 |
| Where is the metadata of uploaded plan documents? | Same gap as FB-8 | FB-8 |
| Confidence score / AI names uncertainty | Backend already computes `confidence: low/medium/high` per shallow answer (`chat_researcher/models/result.py:35`) — used internally for escalation, never shown to the user | FB-6 |
| AI asks follow-up questions when info missing | **Done** — clarifier agent is a live multi-turn dialog, recently fixed (`07251d0`) with frontend keyboard support | — |

## Standing rules (user-directed)

- **All new features ship behind WorkOS feature flags** (registry `frontends/ui/src/lib/authz/feature-flags.ts`, flags created via the WorkOS connector, runbook `docs/deployment/workos-provisioning.md`). Flags created 2026-07-14 for this session's features: `source-origin-badges` (FB-2; on in Staging+Production), `chat-confidence-chip` (FB-6; on in Staging, OFF in Production pending the live marker-compliance smoke test), `files-metadata-panel` (FB-8 v1; on in Staging+Production). Code gating = Cycle 6. Judgment calls: FB-7 (preview expand button) treated as minor UI polish, not flagged; FB-5 is deployment-config-gated by design (default-off allowlist), not per-org.

## Ranked backlog

### P1 — Chat trust chain (core of beta promise "safe Baurecht research")

- ~~**FB-1**~~ DONE cycle 1 (`2c6d53e`) — `ChatThinking.tsx` no longer filters `knowledge_layer` out of "Selected Data Sources"; labeled "OIB Knowledge Base" / "OIB-Wissensdatenbank" (i18n `dataSource.knowledgeBase`, old single-use `files` key repurposed); attached files still listed separately, spec updated.
- ~~**FB-2**~~ DONE cycles 2–3 (`6a0cac4`, `eb3f8ac`, `5539b44`) — sources are labeled `[KB]`/`[RIS]`/`[Web]` across all paths (shallow-chat citations, deep-research fallback synthesis, AND the LLM-written "## Sources" section via deterministic token injection in `verify_citations`); frontend renders localized badges, fail-open. Cycle 3 also fixed a latent bug (pre-existing token broke fuzzy filename matching on re-parse). Residual note: RIS docs ingested into the session KB surface as `[KB]` on later KB hits (dual identity, defensible). *History:* `[KB]`/`[RIS]`/`[Web]` tokens emitted by `source_origin_token()` in `_format_registry_reference` + `_append_minimal_citation`; frontend parses them into localized badges in report source lists (fail-open to plain text). **Remaining gap (cycle 3):** the normal deep-research path preserves the LLM-written "## Sources" section, so those lines carry no token yet — fix by deterministic token injection into validated lines inside `verify_citations` (it already resolves every line to a registry entry). Note: token must sit AFTER the `[N]` marker or `_CITATION_LINE_RE`/renumbering breaks. Also noted: RIS docs ingested into the session KB later surface as `[KB]` (dual identity, defensible, revisit only if per-document consistency is wanted). *Original rescope rationale (cycle 1):* shallow chat has NO structured citation channel (citations are backend-appended markdown, `_append_minimal_citation`), and in deep research KB citations never enter the SSE citation pipeline at all (`jobs/callbacks.py` `_is_search_tool` matches only web-search tool names; the pipeline regex-scrapes URLs, `CitationSource.url` is a required string, and live + replay parsers are independent). **Decision: implement labeling at the one owned chokepoint both paths share** — `citation_verification.py` source formatting (`_format_registry_reference`, and the shallow `_append_minimal_citation`), emitting a stable `[KB]`/`[Web]` token per source line; frontend `report-citations.ts`/`ReportTab` maps tokens to localized badges, plain text as fail-open fallback. The full structured pipeline (registry-backed citation artifacts, URL-optional `CitationSource`, both SSE parsers) is deferred to **FB-4**, which needs it anyway. Effort: M.
- ~~**FB-3**~~ ALREADY ADDRESSED (triage error, corrected cycle 1) — `InputArea.tsx:726-730` already renders a muted one-line AI-Act disclosure via `inputArea.aiDisclosure` (dictionary `research.ts`, both locales); the initial verification missed it. No change made.
- ~~**FB-4 — Citation click-through to the original passage.**~~ DONE structured path (`feature/structured-citations`): backend `source_entry_to_wire` emits file_name/page/collection/origin on SSE `citation_source` (incl. KB via `extract_sources_from_tool_result`) and shallow WS `sources[]` (preserved like `answer_confidence`). FE `CitationSource` URL-optional; `resolveCitationTarget` prefers structured locators; Belegt-durch + SourcePreview open PDFs when index matches. Residual: Indexed document must still be listed (project docs / base corpus); Archiv indexing remain separate. *Was:* KB can hit but FE only scraped HTTP URLs, so chips lacked openable locators without client filename guessing.
- **FB-5 — Web source allowlist.** Add `include_domains` (Tavily natively supports it) to `TavilyWebSearchToolConfig` + config plumbing, seeded with a curated AT/Baurecht-quality domain list; keeps the agent off low-quality sites. Effort: S–M.
- ~~**FB-6**~~ DONE cycle 4 (`d7e6cc1` backend, `34570ef` frontend) — self-assessed `[CONFIDENCE:low|medium|high]` marker (fail-open, composes with escalation marker), deterministic overconfidence guard (ungrounded high/medium → capped "low", grounding = citation-verification outcome), error/escalation branches surface nothing, delivered via preserved-extra-field channel, rendered as an honest self-assessment `ConfidenceChip` (tooltip: "can be wrong") beside `MemoryNotedChip`. Needs one human smoke test for live marker compliance (same class as the `[ESCALATE_TO_DEEP]` marker note in backlog.md RUNTIME-SMOKE). Follow-up note: shallow prompt now carries two trailing control markers — if a third is added, unify into one control line. *Original rescope rationale: the pre-existing `ShallowResult.confidence` is NOT a real signal* — it's a control-flow proxy ("high" = "certain an error occurred", set only on error/escalation branches; never computed for successful answers; "medium" is dead code). Surfacing it verbatim would mislead. **Decision:** (1) real v1 signal = structured trailing `[CONFIDENCE:low|medium|high]` marker in the shallow prompt, stripped by the node — same fail-open pattern as `[ESCALATE_TO_DEEP]` (no marker → no chip); (2) deterministic overconfidence guard: cap at "low" if citation verification stripped all citations or the registry is empty; (3) error branches emit no chip; (4) delivery via the proven preserved-extra-field channel (`nat_converters._PRESERVED_EXTRA_FIELDS` → `websocket_reconnect.py` → zod `NATSystemResponseMessageSchema` → `ChatMessage`), rendered as an honest "self-assessment" chip beside `MemoryNotedChip` in `AgentResponse.tsx` with explanatory tooltip. Effort: M.

### P2 — Files quick wins

- ~~**FB-7**~~ DONE cycle 1 (`454471f`) — Maximize2 ghost button in `file-preview-pane.tsx` opens the existing `PdfViewerDialog` (gained an optional `src` prop for presigned URLs, backward-compatible); gated to PDFs; i18n both locales; 2 new spec tests.
- ~~**FB-8**~~ COMPLETE cycles 5-11 — v1 metadata surfacing, v2 ingestion-generated tags (enum vocabulary), v3 user-adjustable tags + backfill. Cycle 11 (`e91b9bc`, `d86dd5b`): classify-only `scripts/backfill_document_tags.py` (idempotent, --dry-run/--force, per-row fail-soft; Chroma chunk text preferred, summary fallback; OpenAI-compatible client via BACKFILL_SUMMARY_* env vars); `SummaryStore.update_tags`/`list_collections`; `PATCH /v1/collections/{c}/documents/{f}/tags` validating against `ALLOWED_TAGS` (400 lists invalid values) + BFF PATCH with `project:edit` authz; editable vocabulary-chip popover (Dokumenttyp/Fachbereich groups, optimistic + revert) in the flag-gated metadata panel; TS vocabulary mirror pinned by a backend parity test that fails on drift. Verification: backend 1672/0 (+24), frontend tsc 0 + vitest 2002/0 (+8), ruff clean. *History:* stop stripping user-relevant ingestion metadata; show summary/pages/chunks in the preview pane. **v2 rescoped per user directive (2026-07-14): tags are GENERATED BY THE INGESTION PIPELINE** (auto-classification at ingest time — document type / discipline, e.g. Bebauungsplan, Grundriss, Brandschutz — stored alongside the per-document summary, surfaced in the same metadata panel, then user-adjustable). Composes with FB-15a: ingested images get tagged the same way. **Architecture fixed (scouted + decided 2026-07-14):** (1) separate tag-classification LLM call via a NEW shared helper module used by BOTH backends (also de-dupes the already-copy-pasted summary prompt — existing drift landmine); executor-future pattern beside the summary future, fail-open, own timeout; (2) storage = `tags` JSON column on the `summaries` table, migrated via the `jobs/access.py:294-322` ALTER TABLE/PRAGMA pattern (store has NO migration mechanism); extend `register()/get_all()/AvailableDocument/FileInfo`; (3) controlled taxonomy: document type (Bebauungsplan, Flächenwidmungsplan, Grundriss, Schnitt, Detail, Gutachten, Bescheid, Foto, Norm/Richtlinie, Sonstiges) + discipline seeded from the OIB categories in `standards-catalog.ts`, ≤5 tags; (4) tags ride the per-turn `available_documents` prompt line (researcher.j2 ~103-108, both agent copies) to improve doc selection; (5) surface in the same Files metadata panel (reuse `files-metadata-panel` flag); (6) backfill for already-ingested docs = flagged follow-up (OIB sync is hash-gated, no force path; needs a light classify-only script); (7) v3 = user edit via `PATCH /v1/collections/{c}/documents/{f}/tags` + BFF route (no user-facing PATCH precedent exists in aiq_api — follow maintenance.py's thin-handler→factory→store shape with normal end-user auth). Effort: M (v2) + M (v3 edit).

### P3 — Navigation restructure (directionally clear, confirm IA with team)

- **FB-9 — Project Settings page; fold Overview specs & Members into it; slim sidebar.** No Settings page exists today. Create one, move project-spec content (Overview) and Members there, reduce the rail. Note: `app-sidebar.tsx:144-146` documents a deliberate "Members never dead-ends" choice — revisit consciously. Effort: M–L. *Confirm target IA with the team before building.*
- **FB-10 — Merge Research into chat history as "Deep Research".** Remove the separate `/research` tab; `SessionsPanel` already shows per-session research status icons — extend it with a "Deep Research" label/filter and port anything `ResearchRunsList` shows that sessions don't (job status, expiry). Effort: M.

### P4 — Wizard

- **FB-13 — End-of-wizard conflict check.** *Rescoped per user directive (2026-07-14): structured answers are checked DETERMINISTICALLY* — a typed rule module beside `intake-definition.ts` (zod refinements or predicate functions) encodes unambiguous cross-field contradictions (GK vs floor counts, Hochhaus details vs floors, orphaned conditional answers, new-build vs building age), conservative to avoid false positives, instant client-side on Save, exhaustively unit-tested, i18n explanations. *The LLM checks ONLY free-text fields* (goal_details etc.) against the structured answers — network call skipped entirely when free text is empty; fail-open per the `generate_summary.py` error-code pattern. Merged findings in the ReviewStep with edit links + "Trotzdem speichern" override (= the human stage). Behind `wizard-conflict-check` (Staging on, Production dark). In flight cycle 10.
- **FB-14 — Plan upload (Flächenwidmungs-/Bebauungsplan) with extraction + verification.** New extraction pipeline (upload → parse → prefill `widmung` etc. → user confirms each extracted value). Large; scope with the team first (formats? Wien-specific plan layouts?). Effort: L. *Needs product scoping.*

### P5 — Larger features / needs measurement

- **FB-15a — Standalone image upload via existing VLM-captioning machinery.** *Rewritten after deep discovery (user challenged the original "whole new pipeline" verdict — correctly).* The heavy machinery already exists and is reusable unchanged: `_analyze_image_with_vlm` (llamaindex adapter ~363-476) takes raw bytes and is not PDF-specific; vector store, retrieval, `ContentType.IMAGE` citations ("file, p.N, Image N"), MinIO, and the Files-page `<img>` preview + presign content-types all already support images. Today a PNG fails because it slips through to `SimpleDirectoryReader`, gets UTF-8-garbage-decoded (no `llama-index-readers-file` installed), and is rejected by the binary-content guard (adapter ~1699-1703) before any image code runs. **Delta (M, not L):** (1) new `is_image` branch in `_run_ingestion` (magic-byte sniff like `_looks_like_pdf`; PIL — already a pinned dep — reads bytes; call existing captioner; emit one caption `Document`, ~45-line clone of the PDF-image block at 1637-1683); (2) register the caption as the file's summary so it appears in the per-turn `available_documents` prompt list (summaries are the ONLY per-turn file visibility; files without summaries are invisible to the turn); (3) flip the 6 client-side type gates + env defaults + `_infer_suffix` map (13 touch points inventoried); (4) add server-side type allow-listing — none exists today for ANY type (pre-existing gap worth closing regardless); (5) small image branch for the Files expand-dialog (currently PDF-gated). Prereq: VLM env vars (`AIQ_VLM_*`) — already live in the coolify deployment (`AIQ_EXTRACT_IMAGES=true`, OpenRouter VLM). **Do NOT just flip the accept-list** — without (1)+(2) images upload "successfully" but are silently invisible to chat.
- **FB-15b — True chat-time vision (pixels to the model at answer time).** Remains L/structural and a product decision: with FB-15a users get caption-level answers (one VLM caption at ingestion, one-sentence summary per turn), not pixel-level ("what's the load rating in this photo" won't work). No message-scoped image path to the answering LLM exists anywhere. Decide after FB-15a whether caption-level is enough for beta.
- **FB-16 — Output-cards overview page.** Aggregate all cards of a project outside chat context (candidate synergy with FB-12 Archive). Effort: M. *Where should it live? Clarify with team.*
- **FB-17 — Chat latency tuning.** "Hang forever" is fixed; raw speed isn't: shallow tier runs `reasoning_effort: high`, `max_tokens: 65536`, ≤10 LLM turns (`config_oib_openrouter.yml:62-71,270-271`). Candidate: lighter/faster settings for the shallow tier. *Blocked on runtime measurement — config tuning without live latency data is guesswork.*

### Needs product / human decision (not build-ready)

- **FB-11 — Workflows page.** No spec exists; define what a "workflow" is for users first.
- **FB-12 — Archive page** ("good knowledge" from past projects). Org-scoped Project Memory is a substrate, but the product shape (what gets archived, by whom, browse UX) is undefined.
- **Human smoke test:** deep research end-to-end (post-PR #56) and the fixed chat-spinner paths — all verified statically only.
- **Design-vs-prototype visual diff:** prototype is external to the repo; needs eyes.
- **Wizard question specifics:** ask the feedback author *which* questions to change (revision machinery exists).
- **Open questions from the feedback that are org/ops, not code:** upload speed at scale, where pilot-office data lives, policy for "user uses Piloti as a general LLM".

## Already addressed (do not re-litigate; evidence retained)

- **Deep research broken** → 5-commit hardening pass merged 2026-07-14 (`751476d`, `a4e36f6`, `dc756e6`, `0f032a9`, `973a0cb`); runtime smoke test pending.
- **Chat gives no answer** → fixed Jul 10–13: `ee284c2` (terminal `workflow_error` frame + 180s inactivity watchdog), `61a4858` (WS reconnect churn), `f732054`.
- **Status display while AI works** → `ChatThinking.tsx` (live steps, waiting/interrupted/done).
- **AI asks follow-up questions** → clarifier agent, live and recently fixed (`07251d0`).
- **Design calmer/reduced** → design-language spec + UX audit rounds 1–8 (see `ux_audit_log.md`); pending only external-prototype comparison.
- **Wizard questions revised** → `intake-definition.ts` v2, actively iterated (`b97924a`, `4d76307`).

## Cycle log

- **Cycle 16 — capability-derived image upload + unified credential resolution (2026-07-15, user-driven doctrine):** User challenged (correctly) both the triple-switch image opt-in and the bespoke VLM key env. **Doctrine adopted and written into AGENTS.md: flags = product decisions, env = real infrastructure dependencies, capability DERIVED from the dependency, availability = flag ∧ capability.** (a) `2363e8d`: `vlm_available` rides the existing `/v1/data_sources` capability payload; accepted upload types now derive automatically — images on when `image-upload` flag ∧ VLM configured, off otherwise even if env lists them (closes the silent-failure hole permanently); server-side gate applies the same rule via a TTL-cached fail-closed probe; image rejections explain the missing VLM (i18n). `FILE_UPLOAD_ACCEPTED_TYPES` is now an override, not a requirement. (b) `73fdd23`: shared `resolve_llm_credential()` (BYOK → primary env → fallbacks → **provider inference from base_url host**: openrouter.ai→OPENROUTER_API_KEY etc.) adopted at VLM, embeddings (AIQ_EMBED_API_KEY now real — was a dead compose var), backfill script, consistency-check, generate-summary; the two BFF-invoked routes gained BYOK via forwarded `x-grid-organization-id`; `read_api_key_env` `${...}` guard promoted and applied everywhere; compose comments updated. Follow-ups recorded in code comments: org-BYOK at ingestion needs org identity through /v1/ingest + pre-pool capture; card_llm lacks BYOK (pre-existing). WorkOS `image-upload` flag enabled in Production (user is the smoke test). Verification: backend 1766 passed / 5 skipped, frontend tsc 0 + vitest 2086 / 3 skipped, ruff clean, compose YAML valid. Net for operators: a coolify deployment with just OPENROUTER_API_KEY gets working image upload with zero additional env.

- **Cycle 15 — production 504s + disconnect survival (2026-07-15):** Live-debugging with the user (Network-tab 504 screenshot, backend logs, in-container probes). **504 root causes, layered:** (a) CONFIRMED core deployment issue — custom `aiq-network` in the coolify compose makes containers dual-homed; Coolify's Traefik non-deterministically routes to the unreachable IP (coolify docs + issue #6215) → removed the custom network (`8f13378`; requires full Redeploy, not restart); (b) CONFIRMED code exposure — server-side fetches to the backend had no timeouts and the postgres pool was unbounded → all bounded + summary generation off the save critical path (`fe9f18c`); (c) CONFIRMED differential (user's insight — "chat works fine"): the failing flows uniquely make 2 WorkOS FGA round-trips; worst case pre-fix = 60s membership + 60s FGA = 120s > Cloudflare's ~100s — the exact hang arithmetic; bounded to 30+30=60s with slow-call instrumentation + opt-in FGA cache `GRID_AUTHZ_CACHE_TTL_MS` (`48c4116`). Live probes during a good window showed all legs healthy — post-deploy, any recurrence self-identifies in frontend logs. **Disconnect survival (user requirement: leaving a chat must not kill generation):** production log PROVED the backend completes after disconnect and drops the frames (QueueClosed) — fixes (`d16b791`, `d440625`): disconnect no longer cancels the workflow (supersede-cancel kept); terminal assistant message persisted server-side to the BFF conversation store when the client is gone (deterministic uuid5 id → ON CONFLICT no-op, live-socket recheck, cookie-replay auth, 10s fail-soft); frontend "Interrupted" state is hydration-aware (refetches server history before showing the banner); leaked DeepSeek DSML tool-call blocks stripped from answers with emit_card salvage into the registry; shallow path passes reference_sources so citation sections synthesize. Verification: backend 1794 passed / 5 skipped / 0 failed; frontend tsc 0 + vitest 2070 / 3 skipped / 0 failed; ruff clean. **User runbook:** merge #58 → full Redeploy in Coolify → retest save (if anything still fails, frontend logs now name the leg); rotate the aiq DB password (older logs contain it).

- **Cycle 14 — review round 2 on PR #58 (2026-07-15):** Six finder angles over the post-merge fix diff → 9 findings (6 correctness, 2 cleanup, 1 pre-existing quirk hardened), all fixed (`b076743`, `b83c755`): marker signals now extracted from the SAME answer message the chat node finalizes (the one true refactor regression, caught by two angles independently); extraction sentinel redesigned to `escalation_requested: bool|None` (unconditional-True bug gone, fallback preserved); marker detection tail-anchored per the prompt contract (quoted mid-text markers no longer trigger escalation or get stripped); no-result citation filter constrained to single-line ≤300-char bodies (real "No results found, however…" content survives); assumptions-only salvage now opens edit mode AND `runSave` merges `initialProfile.assumptions` so edit-mode saves no longer wipe them (found while fixing: PUT replaces the whole profile and assumptions never round-trip through wizard answers); pruning applied on load/draft-restore; redaction fallback scrubs credentials but keeps bare paths readable; `_build_agent` reused in the override branch; salvage double-parse dropped. Review also POSITIVELY cleared: serde strict-mode restores old permissive checkpoints safely (blocked types degrade to dicts and re-coerce — the state.py comment overstated the risk), marker move otherwise byte-exact, lazy-resolution locking sound, redaction coverage complete, no doc obligations violated. Verification: backend 1777 passed / 5 skipped / 0 failed, frontend tsc 0 + vitest 2039 / 3 skipped / 0 failed, ruff clean.

- **Cycle 13 — post-merge investigation + fixes (2026-07-15):** User reported "error when saving the project brief" post-merge + a production log. Three-agent diagnosis: **(1) the save error is NOT caused by PR #57** — the profile PUT path is byte-identical to pre-branch develop; most likely environmental (Postgres migration drift on `profile*` columns in the coolify deployment → 500 → `intake.errors.saveFailed`); needs the frontend container log (`[api] Unhandled error in PUT /api/projects/[id]/profile`) or the PUT's HTTP status to confirm — alternatively the user saw the new conflict panel ("Trotzdem speichern") and read it as an error. **(2)** The `[CONFIDENCE:]`/`[ESCALATE_TO_DEEP]` lines in the log are pre-strip verbose-callback output — the delivered message was clean; but a latent streaming leak (emit_final_report pre-strip) and a prompt gap (meta signal never rendered) were real. **(3)** Log findings: DB passwords printed via `db_url[:50]` truncation at 13 sites; deep-research "0 tools" startup warning = build-order race with a REAL latent sync-path bug (empty tool list captured in closure); RIS planner json_schema not honored by served model (parse errors not retried); RIS "No documents found" outputs captured as `[RIS]`-labeled citations (trust-chain pollution, shared-filter vocabulary gap); langgraph checkpoint serde unregistered-type warnings (future-blocking). **Fixes landed** (`daa3c16` credential redaction via make_url hide_password at all sites — ACTION human: rotate the `aiq` DB password, it is in existing logs; `d3bda2e` lazy deep-research tool resolution + bounded RIS-planner ValidationError retry + conservative no-result citation filter; `c956bb4` markers extracted+stripped inside the shallow agent pre-emit with structured signal carriers + `requires_sources` rendered into researcher.j2 for deterministic meta suppression; `5f5578b` explicit serde allow-list [IntentResult, DepthDecision, ShallowResult, AvailableDocument] + round-trip test — NOTE: strict mode, new state models must be added to the list; `c2460c9` wizard: legacy-profile lenient salvage + warning banner (was: silent blank + overwrite), fail-open pre-save gate, stale-conditional-answer pruning on edit, data-driven rule copy, non-finite number rejection). Verification: backend 1710+61 passed / 0 failed, frontend tsc 0 + vitest 2032/0, ruff clean. Deferred with rationale: structured-output method switch to function_calling (needs live model validation), meta-with-tools contract decision (needs product input — see docs/architecture/meta-vs-research-contract.md).

- **Cycle 12 — comprehensive review + fixes (2026-07-14/15):** xhigh review of `develop...HEAD` (10 finder angles, adversarial verification, gap sweep): 32 candidates → 15 reported findings (3 refuted en route). Fixes landed: `873754b` (frontend: image types removed from shipped defaults — env-opt-in paired with `AIQ_VLM_*`, since fail-open flag + unconfigured VLM = guaranteed silent upload failures; tags PATCH now syncs parent state so saves no longer revert on reselect; `answer_confidence` zod hardened with `.catch(undefined)` so version skew can't drop whole chat messages; SessionsPanel quick close/reopen race fixed; consistency-check authz aligned to `project:edit`; 15s TTL cache restores the zero-backend-call steady state for all-terminal document reads), `952ce1f` (backend: tag persistence decoupled from summary success via deterministic fallback summary — fixes silent tag loss + FileInfo/store divergence + orphaned tag futures at all 3 ingest sites; text extracted once per file in foundational_rag; MAX_TAGS enforced on the PATCH route; backfill exits 1 on per-row failures; store not marked initialized when the tags migration fails (retries next call); images downscaled to 1568px before VLM calls), `e213b94` (docs: env vars, API routes, tags schema/migration documented per AGENTS.md obligation; compose defaults reverted to non-image). Verification: backend 1692/0 (+20), frontend tsc 0 + vitest 2013/0 (+11), compose YAML validated. Remaining from review (accepted/known): confidence-chip semantics are grounding-not-correctness (design consequence, tooltip already accurate); dev-only SQLite multi-process migration race (prod Postgres safe); cleanup themes (flag prop-drilling → provider at next flag, shared summary+tags orchestration helper, fence-stripper unification with json_utils, Chip reuse in tags editor, dead profile-based consistency wrapper, marker-protocol unification at a third marker) — candidates file retained in session scratchpad, all low-risk refactors for a quality pass. Pre-existing doc gap found: `POST /v1/generate-summary` undocumented in docs/api/python-endpoints.md.

- **Cycle 0 (2026-07-14):** 7 parallel verification agents ran; this file created.
- **Cycle 1 (2026-07-14):** FB-1 + FB-7 implemented (`2c6d53e`, `454471f`); FB-3 found already shipped (triage corrected). Verification: `tsc --noEmit` exit 0; full `vitest run` 1919 passed / 3 skipped / 0 failed (suite grew past the 1531 baseline with PR #56; zero regressions). In parallel, a wiring-map research pass rescoped FB-2 (see item) and surfaced a pre-existing gap now folded into FB-4: `knowledge_retrieval` never triggers citation artifacts in deep research (`jobs/callbacks.py` `SEARCH_TOOL_PATTERNS`), so KB sources are invisible in the live citation feed. Next: Cycle 2 = FB-2 (chokepoint labeling per decision above).
- **Cycle 2 (2026-07-14):** FB-2 core landed (`6a0cac4` backend tokens + tests, `eb3f8ac` frontend badges + tests). Verification: backend 207 passed (citation_verification + shallow_researcher suites), ruff clean; frontend tsc 0, vitest 1925/3 skipped/0 failed. RIS labeled `[RIS]` (identified via `ris_*` tool name or `ris.bka.gv.at` host; false-positive guard test included). Remaining deep-research-common-path gap recorded on FB-2 → Cycle 3.
- **Cycle 3 (2026-07-14):** FB-2 completed (`5539b44`) — origin tokens now injected into validated LLM-written source lines inside `verify_citations` (new `entry_for_url`/`entry_for_citation_key` registry getters; idempotent; token stripped before identity matching, which also fixed a latent fallback-re-parse bug). Verification: 365 backend tests passed (citation_verification + shallow + deep researcher suites), ruff clean; frontend untouched (token shape identical to cycle 2). In parallel, FB-6 research revealed the existing confidence field is a misleading control-flow proxy — FB-6 rescoped with a fixed architecture (see item). Next: Cycle 4 = FB-6.
- **Cycle 4 (2026-07-14):** FB-6 landed (`d7e6cc1`, `34570ef`) — see item. Verification: backend pytest 1258 passed / 0 failed, aiq_api WS suites 31 passed, ruff clean; frontend tsc 0, vitest 1936 / 3 skipped / 0 failed. In parallel: FB-5 scoped (langchain-tavily supports `include_domains` at construction; hard filter, no boost → decision: ship the mechanism default-off, domain curation is a product decision — no seed list exists anywhere in the repo); FB-8 scoped (per-document summaries already persisted in the `summaries` SQL table with zero HTTP exposure; v1 = unblock existing data through reconcile-status/service strip points, no migration; v2 labels = clean BFF side-table feature; landmine: `documents.metadata` jsonb is internal-only with an overwrite race — don't reuse it); FB-10 scoped (remove nav item + redirect `/research`; move the server-truth runs list into the chat-history panel as a collapsed "Deep Research" section backed by `GET /v1/jobs/async/jobs` — pure local-storage badges would lose cross-device/orphaned-job discovery); FB-15 rediscovered at user's prompting and rewritten (see FB-15a/b — original "L / new pipeline" verdict was wrong). Next: Cycle 5 wave = FB-5 mechanism + FB-8 v1 + FB-15a.
- **Cycle 5 (2026-07-14):** FB-5 mechanism landed (`5435d54` — `include_domains`/`exclude_domains` on `TavilyWebSearchToolConfig`, default-off, commented examples on both yml tool instances, new test package 8 passed; domain-list curation flagged for product). FB-8 v1 landed (`8ee5901` backend/BFF, `b4bd7a4` UI — route-level summary merge from SummaryStore, fail-open; BFF carries summary/pageCount/chunkCount/contentTypes, still strips internal jsonb; preview pane renders summary + MetaRows; backend 63 passed, frontend vitest 1945/0. Intended behavior change: document list now makes one fail-open backend fetch per collection even with no in-flight rows). WorkOS flags provisioned via connector per new standing rule.
- **Cycle 6 (2026-07-14):** flag gating landed (`d0185de`) — `sourceOriginBadges`/`chatConfidenceChip`/`filesMetadataPanel` in the registry, render-site gates via the established server-computes→prop-drills pattern (chat route split into server shell + client body to enable it), provisioning runbook updated, 4 new flag-off specs; tsc 0, vitest 1949/0. Next: Cycle 7 = FB-8 v2 ingestion-generated tags.
- **Cycle 10 (2026-07-14):** FB-13 landed (`e541d29` backend, `f6837c7` wizard) under the REVISED user-directed architecture — deterministic rule module `intake-consistency.ts` (3 conservative rules: GK1-3 with >4 above-ground floors per OIB RL2 Begriffsbestimmungen; Fluchtniveau >22m/11-22m vs physically-impossible floor counts; orphaned conditional answers driven by the intake definition's own visibility conditions; 19 unit tests, i18n-keyed findings) + LLM confined to the two free-text fields (`goal_details`, `hohe_gebaeude_details`), enumerated dynamically, call skipped when empty/<10 chars, fail-open per the generate_summary error-code pattern. ReviewStep: instant deterministic findings, merged AI findings, per-finding stage links, "Trotzdem speichern" override (= the human stage). Behind `wizard-conflict-check` (Staging on, Production dark). Verification: backend 11 passed + ruff clean (pre-existing plugin.py I001 confirmed pre-existing); frontend tsc 0, vitest 1994/0 (+26). A first everything-to-LLM attempt was stopped mid-flight (user-corrected architecture); its scaffolding was salvaged, its contract rewritten.
- **Cycle 11 (2026-07-14):** FB-8 completed (see item) — backfill script + user-adjustable tags. First attempt died on a session limit leaving nothing; retry after reset landed cleanly. **The build-ready backlog is now empty** — every remaining item needs human/product input: smoke tests (deep research, confidence marker, image upload, wizard check), LICENSE decision, web-allowlist curation, FB-9 Settings IA, FB-11 Workflows spec, FB-12 Archive shape, FB-16 cards-overview placement, FB-17 latency measurement, FB-14 plan-upload scoping, FB-15b chat-time vision decision.
- **Cycle 9 (2026-07-14):** FB-10 landed (`6e8a3a3`) — Research nav item hidden + `/research` server-redirects when `research-in-chat-history` is on (legacy tab fully preserved under flag-off); SessionsPanel gained a collapsed server-backed "Deep Research (N)" section via `listResearchRuns` (covers headless/cross-device jobs, session-title labels with untitled fallback, `?job=` deep links, thinking-tab link for failed runs) + a quiet "Deep Research" chip on research-carrying sessions. Verification: tsc 0, vitest 1968/0. Flag: Staging on / Production off pending IA sign-off. Note: a transient git race with the parallel header-removal task was detected and self-corrected (commit re-staged to exactly 15 files); coordinator verified.
- **Header removal (2026-07-14, user directive):** `e25fef3` — 283 files stripped of SPDX/Apache header boilerplate (NVIDIA + Grid variants, hash and HTML comment styles, shebangs preserved); attribution consolidated in new top-level `NOTICE` (Apache-2.0 derivation from the NVIDIA NeMo Agent Toolkit / AIQ blueprint). frontends/ui had zero headers; no CI header enforcement existed; backend suite exactly at baseline (1637/5 skipped). **FLAG for human: the repo has NO LICENSE file** — NOTICE references Apache-2.0 heritage but the project's own license is undeclared; decide and add one.
- **Cycle 8 (2026-07-14):** FB-15a landed (`ab7ac11` backend, `4484e6b` frontend/BFF) — magic-byte image detection, VLM caption Document (PDF-image machinery reused), caption→summary/tags via the cycle-7 module (caption-as-summary fallback so images are never invisible), all type gates flipped, dual-side `image-upload` flag gating (client accept-list + server 400), general server-side extension allow-list added (closes the pre-existing no-server-validation gap), image mode for the large preview dialog. Verification: backend 1637/0, frontend tsc 0 + vitest 1961/0, ruff clean. Flags: `image-upload` Staging on / Production dark pending smoke test; `research-in-chat-history` provisioned for cycle 9 (same posture). Needs human smoke test: one real image upload end-to-end (VLM live).
- **Cycle 7 (2026-07-14):** FB-8 v2 landed (`e05f203` backend, `f051371` BFF+UI) — shared `document_classification.py` module with the vocabulary as a GLOBAL ENUM (immutable `DOCUMENT_TYPE_TAGS`/`DISCIPLINE_TAGS` tuples, `ALLOWED_TAGS` frozenset; prompt BUILT FROM the constants; deterministic off-vocabulary drop with an explicit Feuerschutz→dropped test + prompt-drift guard). Summary prompt de-duplicated into the same module (both backends now delegate). `tags` column with fresh+legacy migration on the summaries store; tags ride the per-turn document lines in both researcher.j2 copies; chips in the flag-gated Files metadata panel. Verification: backend 1412 passed / aiq_api 213 passed / ruff clean; frontend tsc 0, vitest 1953/0. **Known follow-ups:** (a) classify-only backfill script for already-ingested docs; (b) tags currently persist only when a summary exists (they ride the summary upsert — if summary generation fails but tagging succeeds, tags are dropped; acceptable v1 coupling, revisit if it bites); (c) planner.j2/orchestrator.j2/source_router.j2 also list documents and could carry tags (deliberate scope cut this cycle); (d) v3 user editing via PATCH validating against the same `ALLOWED_TAGS`.
