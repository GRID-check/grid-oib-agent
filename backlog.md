# Overnight Run Backlog — live ranked queue

> Loop state file. Re-triaged every cycle. Tiers: 1=Security, 2=Bugs, 3=Stability, 4=Features, 5=Docs.
> Item ids are stable (`T<tier>-<n>`). Demoted/blocked items keep their id with a note.

## Tier 1 — Security

- ~~**T1-1**~~ DONE cycle 1 — typed `NoOrganizationError` (403, isAuthzError-compatible) + `requireAuthorizedPageSession()` for the 8 page/action sites; zero API-route edits; contract spec added. Residual (accepted): ~17 bare-call API routes return JSON 500 (not 403) for no-org — strictly better than the old 307→HTML; follow-up **T3-6**.
- **T1-2** [FLAG — human] `deploy/.env` contains live API keys on disk (OpenRouter, WorkOS, Tavily…). Cannot rotate unattended. ACTION REQUIRED (human): rotate keys, move to a secret store. Logged, not fixable in-loop.
- **T1-3** [FLAG — human] `GRID_INTERNAL_API_TOKEN` compose default is `grid-internal-dev-token`. Endpoint fails closed when unset, but a well-known default in compose is weak for prod. Human should set a real value in `deploy/.env` for any shared deployment. Consider warning log when the default value is detected.

## USER-PINNED PRIORITIES (2026-07-05 ~01:00 — override tier order where stated)

- **PIN-1 Deletion pipeline must be PERFECT.** Round-1 fixes DONE (commit 3727a69). Adversarial re-audit ran (P6) → NOT perfect; round-2 fix agent (P8) DIED on session limit before implementing. **REMAINING FINDINGS (round 2, do next — these are real data-safety/GDPR-erasure bugs):**
  - **DEL-F1 (HIGH) MinIO silent partial delete.** `frontends/ui/purger/minio.js:36-44` — `DeleteObjectsCommand` (Quiet:true) returns 200 even when individual keys fail; `.Errors` never inspected → orphaned objects + pointer deleted = unrecoverable + GDPR compliance failure. FIX: throw if `response.Errors?.length > 0`.
  - **DEL-F2 (HIGH) restore-after-partial-purge → hollow restore.** `frontends/ui/src/app/api/projects/[id]/restore/route.ts:30` matches `status='pending'` only, but `markFailed` returns non-terminal failures to 'pending'. Admin restores a project whose Chroma/MinIO were already destroyed → gutted resurrection, purge never resumes. FIX: restore requires `status='pending' AND claimed_at IS NULL` (or attempts=0); 409 `PURGE_IN_PROGRESS` otherwise; recently-deleted.tsx shows purge-state not Restore for attempted rows.
  - **DEL-F3 (MEDIUM) backend collection_deleted:false ignored.** `purge-project.js:55` only checks res.ok; `maintenance.py:126-130` returns 200 with `collection_deleted:false` on real failure → orphaned Chroma collection. FIX: maintenance.py returns an unambiguous status (deleted/not_found/failed); caller throws on failed.
  - **DEL-F4 (MEDIUM) unreclaimable 'purging' row on 10th-attempt crash.** `db.js:34-36` stale-reclaim ANDs `attempts < MAX_ATTEMPTS`; a crash after the 10th claim strands the row in 'purging' forever, and `deletions/route.ts:36` filters `['pending','failed']` so it's invisible to admins. FIX: reclaim/surface terminal 'purging' rows.
  - **DEL-F5 (MEDIUM) TOCTOU single-shot + FALSE docstring.** `purge-project.js:2-6` claims the queue row "stays locked" across the purge — false (Phase-A claim tx commits, releasing FOR UPDATE; Phase B is a new tx; only 'purging' status guards). Hold re-checked once before first external step; residual window = full duration of external calls, undocumented. FIX: rewrite docstring to the real guarantee; re-check hold before EACH external step (backend/MinIO/WorkOS); document residual window.
  - **DEL-F6 (LOW-MED) unescaped LIKE wildcards in backend job purge.** `maintenance.py:67-71` `pattern=f"%{collection_name}%"` — token holder can send `collection_name="%"` to wipe ALL orgs' job_events/job_access/job_info. FIX: escape `%_\` or validate `^proj_[0-9a-f-]+$`.
  - **DEL-F7 (LOW) isNotFound regex too broad.** `purge-project.js:13-15` `/not found/i` treats "Organization not found"/"upstream host not found" as successful WorkOS delete → FGA resource leak. FIX: match only `status===404`.
  - **DEL-F8 (LOW) last_error stored but never surfaced** (deletions/route.ts omits it; recently-deleted.tsx shows generic copy). **DEL-F9 (LOW) backend token compare `!=` not constant-time** (maintenance.py:47). **DEL-F10 (LOW) db.js SQL entirely untested** (claimNext backoff/NOT EXISTS, stale-reclaim, releaseHeld refund, markFailed CASE) — F4's bug is exactly what this gap hides.
  - Attacks that HELD (do NOT re-fix): releaseHeld loop, backoff collapse/overflow, restore-vs-claim race, partial unique index, SQLi, token-guard env matrix, retry idempotency. Round-1 fixes are sound.
  - **ROUND-2 STATUS (done directly by orchestrator after P8 died on session limit):** DEL-F1 (MinIO .Errors → throw), DEL-F2 (restore requires claimed_at IS NULL + 409 PURGE_IN_PROGRESS), DEL-F3 (backend returns status deleted/not_found/failed; purger throws on failed) + per-step hold re-check + corrected false docstring, DEL-F6 (LIKE wildcards escaped + ESCAPE clause — closes cross-org job wipe via "%"), DEL-F7 (isNotFound = status===404 only). Verified: tsc 0, purger 5/5, py_compile+ruff clean.
  - ~~**DEL-F4**~~ DONE cycle 17 — added `reapStranded(sql)` to `purger/db.js` (marks `status='purging' AND attempts>=MAX_ATTEMPTS AND claimed_at < now()-15min` → `'failed'`, preserving existing last_error) and wired it into `index.js` `tick()` before the drain. A crash on the FINAL purge attempt no longer strands the row forever/invisibly — it terminalizes to 'failed' (surfaced by the admin list) without an 11th destructive attempt. Rows stranded BELOW max are already reclaimed by claimNext's stale branch (unchanged).
  - ~~**DEL-F10**~~ DONE cycle 17 — new `purger/db.spec.mjs` (9 tests, Vitest, same faked-tagged-template harness as purge-project.spec.mjs): claimNext (backoff + stale-reclaim + NOT-EXISTS holds + attempts cap + FOR UPDATE SKIP LOCKED + null path), reapStranded (SQL shape + count + no re-attempt), releaseHeld refund, markPurged, markFailed CASE + 2000-char truncation, markFailedPermanent. Verified: purger suite 14/14 in grid-tsc Docker image.
  - ~~**DEL-F9**~~ DONE cycle 18 — `maintenance.py` `_require_internal_token` now uses `hmac.compare_digest` (missing header coerced to "") instead of `!=`, closing the token-timing side-channel. Added `test_maintenance_auth.py` (6 tests) locking fail-closed behavior (unset→403, dev-default-outside-dev→503, wrong/missing→403) + accept paths. Verified: py_compile + ruff clean, pytest 6/6.
  - ~~**DEL-F5/F8**~~ DONE cycle 19 — `/api/deletions` now projects `lastError`; `recently-deleted.tsx` renders it (mono, destructive-tinted) beneath the failure line for failed rows. Admins can now see WHY a purge failed instead of generic "contact support". Verified: grid-tsc Docker tsc exit 0.
  - PIN-1: ALL code findings closed (DEL-F1–F10). Only remaining PIN-1 work = a final clean adversarial re-audit to certify "perfect" (candidate for a later cycle / human sign-off). T1-2/T1-3 remain human-only (rotate live secrets / set real internal token).
  - NOTE (deliberate, deferred): admin deletions list filter left as `['pending','failed']` — the reaper converts stranded rows to 'failed' (already surfaced), so adding transient 'purging' rows to the list would be a UX change needing product input, not a bug fix. Flagged for human if in-progress-purge visibility is wanted.
- **PIN-2 Memory-system audit.** Full audit of Project/Org Memory (schema, internal endpoint token handling, tenancy on every query, digest injection bounds, remember-tool input handling, panel authz). Same rigor as the deletion audit. Schedule immediately after P1 integration.
- **PIN-3 High-quality docs for the ENTIRE app.** Beyond P3's refresh: a coherent docs set (architecture, subsystems, ops/runbook, API surfaces, onboarding). Multi-stream docs effort once P3 lands (avoid file collisions). Quality bar: a new engineer can onboard from docs alone.

## Tier 2 — Bugs

- **T2-CIT1** [OPEN — measured 2026-07-28] **Quote verification is blind to meaning-inverting micro-elisions.** `verify_quoted_spans` allows a verified quote to skip up to `_QUOTE_MAX_ELIDED_GAP = 6` characters of passage text. Measured against the real OIB corpus, elision detection is a step function on that budget: splices dropping ≤6 chars are caught **0%** of the time, ≥8 chars 100%. German legal prose inverts on short words, so a quote that silently drops `nicht` (5 chars) — or `kein`, `nur`, `außer` — passes as verbatim, turning an OIB prohibition into a permission while the answer presents it as a checked, confidence-carrying quotation. Worst case family scored 13/22 wrong. NOT fixable by tuning: gap=4 still misses a 4-char elision *and* introduces false positives (0.4%), and the other two constants are already optimal (see `docs/architecture/quote-verification-calibration-2026-07.md`). FIX: a polarity-aware check that disqualifies eliding a negation/quantifier token regardless of length — i.e. compare the *set* of polarity-bearing tokens in the quote against the aligned passage span, independently of the character budget.

- **T2-10** [OPEN — suspicion from 2026-07-21 adversarial review, pre-existing] The deep-research job-submission chat path (`use-websocket-chat.ts` `resolvedJobId` branch) may share the orphaned-streaming-bubble structure fixed for queue rejections: answer text streams as deltas before the terminal `deep_research_job_id` extra, and the terminal handling never calls `stopStreaming()` (comment says the deep-research SSE releases it). Verify whether an SSE failure/early exit leaves a permanently "streaming" bubble; if so, apply the same finalize-on-terminal defense.

- **T2-7** [FIX IMPLEMENTED 2026-07-16, pending commit] **Model overrides silently no-op: every LLM call keeps hitting the YAML model (deepseek).** Prod symptom: logs print "Model override active: agent group intent -> x-ai/grok-4.5" / "Applying model overrides: {'shallow_research': ...}" but OpenRouter only ever saw deepseek/deepseek-v4-flash. ROOT CAUSE (empirically confirmed against NAT 1.7.0 + langchain-openai 1.2.2): NAT's `openai_langchain` applies `patch_with_retry` (OpenAIModelConfig is a `RetryMixin`, `num_retries=5`), which wraps EVERY public method (`ainvoke`, `bind`, `bind_tools`, …) as `types.MethodType(wrapped, original)` in the instance `__dict__`. `override_model()` used pydantic `model_copy(update={"model_name": ...})`, which shallow-copies `__dict__` — the copy kept the original's bound methods, so calls on the "overridden" copy executed against the original instance and sent the original's model. Logs showed the copy's field (grok), masking it. FIX: `model_overrides.py::_rebind_instance_method_patches()` rebinds instance-level `MethodType`s to the copy after `model_copy`; regression test simulates the NAT patch pattern and asserts the copy's call returns the new model. Verified: 26/26 `test_model_overrides.py` green + full-fidelity NAT repro (wire payload captured at httpx transport: buggy=deepseek, fixed=grok for ainvoke/bind/bind_tools). NOTE: worth filing upstream against `nvidia-nat` (patch_with_retry + pydantic model_copy incompatibility).
- ~~**T2-8**~~ RESOLVED by PR #75 (`9ea2696`, 2026-07-16) — root cause was `create_agent(response_format=ProviderStrategy(strict=True))` binding a strict json_schema on every tool-loop turn; constrained decoders satisfied the `ResearchNotes` schema on turn 1 with zero tool calls. HEAD uses `DeferredStructuredOutputMiddleware` (schema applied only on the final no-tool-call turn) for researcher and planner, plus a fenced-JSON recovery fallback and empty-note guard in `tools/research.py`. Regression coverage added in `tests/aiq_agent/agents/deep_researcher/test_prompt_tool_contract.py` (contract-field pin). Mechanism confirmed live against OpenRouter/`deepseek/deepseek-v4-flash` (2026-07-21): strict schema on the tool turn reproduced the exact symptom (schema-valid ResearchNotes, empty notes/sources, zero tool calls); the deferred pattern restored the `knowledge_search` call.
- ~~**T2-9**~~ RESOLVED by PR #75 (`9ea2696`, 2026-07-16) — the orchestrator prompt's `## Available Tools` was rendered from `tool_set.tools_info` (all source tools) instead of the bound orchestrator tools, so the model called advertised-but-unbound tools. HEAD renders only bound tools as callable and lists source tools under a "not directly callable" section. The buggy surface (rendered prompt) had zero test coverage — now locked by `test_prompt_tool_contract.py` (verified to fail against the pre-fix prompt rendering).

- **T2-6** [AUDITED cycle 8 — FLAGGED for human, code is YOUR UNCOMMITTED WIP so not touched] Deletion pipeline audit verdict: **needs-fixes, not dangerous**. Solid: tenancy (org from trusted DB row), SQL-enforced UTC grace period (GDPR-capped), precise MinIO prefix scoping, idempotent ordered steps, `project:manage` gate, real tests. FIX BEFORE SHIPPING: (1) legal-hold TOCTOU — hold checked only at claim time (`purger/db.js:19-38`), not re-checked inside `purgeProject` before destruction; (2) `GRID_INTERNAL_API_TOKEN` well-known default + `maintenance.py` accepts it (reject the known default outside dev); (3) non-`project` entity types poison the queue (10 futile retries, no alert); (4) deletions list omits `requestedBy` (audit-trail gap); (5) no retry backoff (outage burns all 10 attempts).

- ~~**T2-5**~~ DONE cycle 2 — added `signingS3Client` to the spec's s3 mock; suite 3/3 green; confirmed no other spec mocks `@/lib/s3`.
- ~~**T2-1**~~ CLOSED cycle 3 — already fixed before this run: `.env` no longer uses `${...}` (warning comment present) AND `config_validation.py:_read_api_key_env()` treats `${...}` literals as unset. Cycle output: ported the warning comment to `.env.example` (regression prevention for new deployments).
- ~~**T2-2**~~ DONE cycle 4 — root cause was deeper than "English-only": the structured `shallow_result.escalate_to_deep` was NEVER populated on success (always None), so escalation hinged on 10 guessed prose phrases. Fix: prompt-mandated language-independent `[ESCALATE_TO_DEEP]` marker (fail-open: absent marker = today's behavior), detected+stripped in `shallow_research_node`, populating the existing structured path; keyword tail-match extracted to a tested helper and kept as fallback. 11 new unit tests. NOTE: marker emission needs one live-run observation (LLM compliance) — added to RUNTIME-SMOKE.
- ~~**T2-3**~~ CLOSED cycle 5 — already deleted 2026-07-03 (commit `2570b1b`, `/v1/ingest` ported to aiq_api, entry point removed, tests relocated). Cycle output: corrected the stale claim in `docs/architecture/backend-deep-dive.md`. Residual: ~11 historical docs still mention the old package (informational only) → **T5-3**.
- ~~**T2-4**~~ DONE cycle 6 — summary generation stays best-effort (always 200) but is now diagnosable end-to-end: backend returns machine-readable `error` codes (`llm_not_configured` short-circuits before the doomed request / `llm_request_failed` / `llm_response_malformed`), BFF logs + forwards the code and can no longer clobber an existing summary on failure, wizard console.warns (flow unchanged). Backend tests 6/6 incl. new no-key test.

## Tier 3 — Stability

- ~~**T3-8**~~ DONE 2026-07-16 — optional durable LangGraph checkpointing wired into the async deep-research job path (`5bea711`): `deep_research_agent.checkpoint_db` (env `AIQ_DEEP_CHECKPOINT_DB`), `thread_id=job_id`, `durability="async"`. A worker crash no longer silently loses execution state. Resume is manual-resubmit-based, not automatic (`submit_agent_job` still rejects a duplicate `job_id`, and a resubmit layers new input onto the checkpointed thread rather than continuing exactly from the interrupted step) — see `docs/architecture/backend-deep-dive.md` §9.
- ~~**T3-9**~~ DONE 2026-07-16 — signed `X-Grid-Request-Context` envelope (`6462f98`) consolidating every cross-cutting header (org/user/project id, collection scope, project context/memory, model overrides, budget, disabled sources, + the new structured `bundesland` field) into one HMAC-SHA256-signed object, built once (`frontends/ui/src/lib/request-context.ts`) and sent by all three submission paths as a dual-write alongside the still-unchanged individual headers. Fail-closed enforcement middleware (`c0be85c`) rejects authenticated JWT callers on workflow endpoints (`/websocket`, `/v1/jobs/async/submit`, `/v1/internal/workflows/submit`, `/generate`) that lack a valid envelope; anonymous mode and internal-token calls are exempt. Cross-language contract fixture (`tests/fixtures/grid_request_context.json`) pins the wire format on both sides so a producer that forgets/misencodes a field fails a test instead of degrading silently in prod.
- ~~**T3-10**~~ DONE 2026-07-16 — `register_summary()`'s deterministic fallback ungated from tag-classification success (`7bc5cc7`) + a `reconcile_collection_summaries()` backstop running at the end of every ingestion job (`42a4fa3`, all callers: Knowledge API, `oib_sync`, future callers) backfills any document missing a `summaries` row. "Ingested ⇒ visible" now holds for every ingestion path; the SQL side-table and ChromaDB vector index remain architecturally distinct stores by design, but the double-LLM-failure gap that used to defeat that invariant is closed.

- **T3-1** ~14 pre-existing failing frontend spec files (AuthKit providers, proxy, FileUploadZone, websocket-scope/api-v1 route tests) — environment/mock issues, not regressions (noted 2026-07-04). They block a clean `vitest run` signal for every future change. Triage and fix in batches.
- **T3-2** Backend `_generate_cards` returns None on any failure with only a log — UI can't distinguish "no cards" from "generation failed". Add a `cards_generation_failed` signal on the response (additive, monkeypatch lift + schema).
- **T3-3** DRY base-agent refactor: 7 duplication patterns across 4 register.py files → `common/agent_base.py`. Deferred earlier pending runtime confirmation; safe to do with static verification + full test suite, but HIGH blast radius — keep late in the night, require all backend tests green before/after.
- **T3-4** helm-lint CI job is a no-op (always passes). Make it actually lint or remove the false signal.
- **T3-5** `frontends/ui` tsconfig typechecks test files in `next build` — spec type errors block prod builds (known foot-gun). Consider excluding specs from the build tsconfig (separate `tsconfig.typecheck.json` already exists via Dockerfile? verify) — low risk, high annoyance-prevention.
- ~~**T3-7**~~ DONE cycle 7 — backend suite 944/944 green. 1 impl fix (`prompt_utils.load_prompt` missing `encoding="utf-8"` — real bug: UTF-8 prompts mangle on cp1252 Windows locales), 2 test-cleanup fixes (Windows file-lock on cached sqlite checkpointer connections), 1 stale test (arg-order shift after project_context wiring in jobs/submit.py, `6957324`).
- ~~**T3-6**~~ DONE cycle 20 — actual scope was 19 files/~20 handlers (16 bare + 3 with a 500-only catch), not ~17. Added shared `authzErrorResponse(error)` helper in `lib/auth/require-auth.ts` (maps any authz error → 403, else null) + 5 unit tests; wrapped each broken handler (authz → 403, non-authz still rethrows → 500). Also fixes resource-level authz 500s (requireProjectAccess/role gates), which a middleware path-gate could NOT classify. Verified: tsc 0, vitest 29/29. **Considered & rejected (user-discussed):** middleware org-gate in proxy.ts — can't do per-resource authz, needs an allowlist for org-less routes (org creation/onboarding), runs on Edge; logged as optional defense-in-depth follow-up needing human review + runtime pass. **Follow-up (optional):** migrate the 9 already-correct GUARDED routes to the same helper to de-dup the copy-pasted isAuthzError idiom.

## Tier 4 — Features (only when tiers 1–3 empty)

- **T4-1** Deep-research cards delivery landed (jobs/runner.py → SSE → ReportTab) but is runtime-unverified. Not more feature work — needs a live run; flag for human smoke test.
- **T4-2** Project memory Phase 2 (consolidation/dedup gate, RAG recall) — designed in docs/architecture/project-memory-design.md; NOT for unattended implementation (needs product eyes on quality).
- ~~**T4-3**~~ DONE 2026-07-16 (v1 shipped+wired; chat doorbell 2026-08-21, live shakedown pending) — deterministic 3-stage compliance-check pipeline (`a3f2368`: requirement profile per Richtlinie → batched evidence check → matrix/report, no LLM calls in stage 3) registered as the `compliance_check` function + dedicated `compliance_llm` role in `configs/config_oib_openrouter.yml` and the `aiq_compliance_checker` `nat.plugins` entry point (`3f1db6a`). ~10-25 LLM calls total for a full 6-Richtlinie check vs. ~300 turns / 20+ min through the open-ended harness. Chat tool on `shallow_research_agent`; live run against a model still pending; see `src/aiq_agent/agents/compliance_checker/README.md`.
- ~~**T4-4**~~ DONE 2026-07-16 — `job.phase` SSE events (planning/research/writing/citation-verification/done, `662b5b7`, detected from existing orchestrator callback events with no change to the `deep_researcher` package) surfaced live in the UI status pill (`7062be8`), plus a per-run completion-token ceiling `GRID_MAX_RUN_COMPLETION_TOKENS` (env, default `0`=disabled) enforced across every LLM call in a job including concurrent researcher workers (`BudgetGuardCallback`).
- ~~**T4-5**~~ DONE 2026-07-16 — golden OIB-compliance eval suite (`b2d3290`, `frontends/benchmarks/oib_compliance/`): 4 hand-authored cases run through the real `chat_deepresearcher_agent` workflow, bounds on wall-clock/LLM-calls/completion-tokens (latency/cost regressions) + checklist-graded answer correctness (no LLM judge). Calibration against a live run still pending (`bounds_calibration_pending: true` in the fixture) — see the suite's README before trusting a failure as a real regression.

## Docs overhaul (PIN-3) — remaining after this session

DONE: enterprise system-overview (SSOT, `docs/architecture/system-overview.md`); ADRs 0008-0013 + flipped 0002-0007 to Accepted; purge 79→44 (35 removed, 4 relocated); new docs cards.md, llm-providers.md, contributing/testing-and-verification.md; AGENTS.md doc-obligation rule; README rewrite.
REMAINING (per triage plan):
- **De-stale ~25 reference docs** — kill the two systemic lies: (a) dead SSE-chat framing (chat is WS-only) across technical-reference/{chat-flow,architecture-overview,authentication-flow,bff-proxy-pattern,collection-scoping}, api/{bff-routes,python-endpoints,websocket-protocol}, user-guides/chat; (b) Kimi-as-default across deployment/{docker-compose,environment-variables,security-config,startup-flow} — make config_oib_openrouter the default, add missing env vars (MINIO_PUBLIC_ENDPOINT, GRID_INTERNAL_API_TOKEN, FRONTEND_INTERNAL_URL).
- **DB truth:** database/schema.md → 9 tables (add project_memory, deletion_queue, legal_holds, project_folders); database/migrations.md → through 0009.
- **Add x-grid-project-memory** to header tables (websocket-gateway, collection-scoping, api/websocket-protocol).
- **4 new docs:** api/internal-memory-api.md, user-guides/memory.md, deployment/runbook.md (purger/legal-holds/backups), and polish architecture/deletion-pipeline.md (relocated raw).
- **Reconcile auth-state contradiction** (architecture/multitenancy-and-auth-spec.md REQUIRE_AUTH=false vs technical-reference auth docs) — needs a code check.
- **Rewrite docs/README.md** as a single-tree index (was three overlapping trees).
- **project-memory-design.md** header still says "Status: DESIGN (not built)" — flip to shipped.

## Backend rough edges (from 2026-07-05 backend inventory — new)
- Duplicate card-gen logic: `cards/generate.py` vs inline `ChatResearcherAgent._generate_cards` — consolidate.
- `otel_header_redaction_exporter.py` raises at import if OTel extra missing (only partially guarded).
- Unused back-compat aliases in runner.py/submit.py; duplicate get_all_sources import in routes/jobs.py.

## Tier 5 — Docs

- **T5-1** `AGENTS.md` — stale: no mention of the typecheck loop, project memory feature, internal API token env, or the two knowledge systems. Update contributor guide.
- **T5-2** `docs/architecture/backend-deep-dive.md` — add the internal-memory-API single-writer change (§ written pre-rework mentions grid_app direct write removal — verify accuracy).
- **T5-3** ~11 historical docs under docs/ still mention `fastapi_extensions` (deleted). Informational-only; sweep in one doc-cleanup cycle (update architecture/ingestion docs to the aiq_api-only path).

## Pilot feedback (Matthias, 2026-09-01) — verified, not yet fixed

Full triage with evidence: [`docs/audit/pilot-feedback-triage-2026-09.md`](docs/audit/pilot-feedback-triage-2026-09.md).
Eight items were fixed in the same pass; these are what is left, ranked by
(user trust × cheapness). Ids are stable (`PF-n`).

- ~~**PF-1 (HIGH, cheap) `view_knowledge_image` is wired into no config.**~~ Closed: declared and bound in `config_oib_openrouter.yml`, locked by `tests/aiq_agent/test_config_tool_wiring.py`. The tool is built, registered, and documented as default-on, but `grep -rn view_knowledge_image configs/` returns nothing and it never appeared there in the history. So the model reads captions *of* plans and never a plan — which is the honest half of "der Agent bezieht die visuellen Planunterlagen kaum ein". Two YAML edits (`functions:` + `shallow_research_agent.tools`), plus a check that the chat model is vision-capable and a token-cost estimate.
- ~~**PF-2 (HIGH) a session upload is answered before it is ingested.**~~ Closed: the composer holds the message until ingest finishes, the turn holds for in-flight files (`GRID_INGEST_WAIT_SECONDS`), and the inventory names what is still being read. No inline vision call on a chat attachment exists at all; ingestion is async on a 2-worker pool with a 180 s per-call VLM timeout, and the send is deliberately never blocked (`InputArea.tsx`, tooltip as the entire signal). Nothing tells the agent the file is still processing either — the per-turn inventory reads the summaries table, written only when the job finishes. Two slices: hold the turn while uploads are in flight; surface in-flight ingest jobs in the prompt inventory (`ingest_status_store` exists and is never read on the chat path).
- ~~**PF-3 (HIGH, data safety) re-upload leaves a downloadable, unsearchable ghost.**~~ Closed: every shelf replaces on re-upload, and migration 0074 makes one live document per `(organization, collection, filename)` a unique index. No content hash, no unique constraint on `(collection_name, filename)`, no server-side existence check. A second upload writes a second row and a second stored object charged to quota, while `_replace_previous_versions` deletes the FIRST row's chunks. Worth fixing before versioning is designed on top of it. Related: `service.ts:988-990` still asserts the ingest endpoint "does not replace" — false since `166dd79`.
- **PF-4 (MED) a mentioned colleague is told only inside the app.** `notifyMentionRecipients` writes an inbox row and publishes a live event; there is no email or push transport anywhere in the product. The most likely honest basis for "@ erwähnen funktioniert nicht", now that the picker defect is closed.
- **PF-5 (MED) move a document between shelves.** A document lives in exactly one retrieval collection and there is no promote/demote path, so re-shelving means delete and re-upload. This is the ONE thing the archive proposal actually needs — the "two competing storage locations" premise is false (ADR-0024: one table, one `scope` column, and the Archiv already rides in every project's retrieval scope).
- **PF-6 (MED) project lifecycle status.** `projects` has no status column. "Aktiv / abgeschlossen" plus the closing form is the valuable half of archive proposal §2, and nothing in the schema resists it.
- **PF-7 (MED) drag & drop into folders.** `PATCH /api/documents/[id]/folder` and a menu action already exist; this is a drag source, drop targets, and telling an internal drag from an OS file drag (the workspace already listens for the latter).
- **PF-8 (MED) folder upload.** No `webkitdirectory`, no directory traversal — a dropped folder yields zero files. The sink is built and ADR'd: `folder_path` reaches ingestion and `knowledge_search(folder=)` filters a subtree by it (ADR-0049). Blocked in practice by **PF-9**.
- **PF-9 (blocker for any bulk upload) the 100 MB batch ceiling applies to project and Archiv uploads.** Only the file-COUNT cap is exempted for durable corpora (`validation.ts:334-353`), so a folder drop of any real Einreichung is rejected wholesale. Also: duplicate detection is client-side and localStorage-backed, so it vanishes in a new browser — any delta feature must be server-side or it is theatre.
- **PF-10 (LARGE) resumable upload.** Nothing exists. The three `@uppy/*` packages are imported by nothing (`project-uppy-upload.tsx` is a plain hidden `<input>`) and `@uppy/xhr-upload` cannot resume regardless. Needs tus or S3 multipart plus a server-side session store, and it collides with an architecture that buffers whole files in the Node process.
- **PF-11 (LARGE, needs an ADR) thumbnails and viewers for CAD/Office.** Both thumbnail generators handle PDF and raster only; DWG/DXF/IFC/TIFF/DOCX/XLSX get none, and the fallback placeholder is correct behaviour. The same wall bounds how far citation-opening can widen — offering to open a format nothing renders moves the silent failure into the viewer.
- **PF-12 [DESIGN — needs a decision, not code] the Herleitung's „Ausgeführt“ row is thin.** The stutter is fixed: each KIND of work is named once. Showing the search QUERIES was built and REVERTED on 2026-09-01 at the stakeholder's direction — do not re-propose it without a new decision. What is open is what a derivation should say instead, if anything. Evidence, so the next attempt starts from facts: the wire already carries the query as a key+values pair the dictionary can phrase, and the per-hit relevance score, `Punkt:` id and excerpt sit unread in `rawPayload`. Anything added must survive `prune-message-for-storage`, or it exists only for the live turn.
- **PF-13 (SMALL, product decision) the card type ramp is 13.5px against 16px prose.** Every step is internally consistent and externally undersized. Re-basing affects all 35 cards at once; charter §A2 now records the question as open.
- **PF-14 (SMALL) let the agent ask which shelves to search.** `ask_user` exists and is instructed, but its description forbids asking what a search could establish, and shelf selection is a UI chip the agent receives and never negotiates. Worth revisiting now that a subject no longer costs the turn its building code.
- **PF-15 (product work, named in the code) user-defined categories for the Archiv.** The chip filter over real tags exists and says so out loud: "no custom-category creation (that needs product work)".
- **PF-16 (SMALL) per-organization ingest blacklist.** Only a deployment-wide extension allow-list and a global platform exclusion file exist. The org `settings` jsonb is the obvious empty home.

## Blocked / waiting on human

- **RESEARCH-403** — root cause traced to two candidate throw sites; needs one runtime data point (error `code`: FORBIDDEN vs BACKEND_ERROR). T1-1 may fix the latent-redirect variant of it. After T1-1, re-test needed by human.
- **RUNTIME-SMOKE** — all session fixes (WS projectId, cards sync path, MinIO presign, summary preservation, memory) verified statically; need one human-driven pass.
