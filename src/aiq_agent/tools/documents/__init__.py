"""The working directory: where a chat turn writes a document instead of describing it.

Per conversation, never a ``documents`` row. Filing a draft into the project is
a separate, human-gated path (see ``AGENTS.md`` one level up).
"""
